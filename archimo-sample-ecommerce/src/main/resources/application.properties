# Optional: used by Stripe Feign client (OrderService payment path)
stripe.api.url=https://api.stripe.com
stripe.secret-key=${STRIPE_SECRET_KEY:}

# Optional: when set, registers OpenAPI-generated Gravatar OkHttp beans (customer avatar lookup)
gravatar.api-key=${GRAVATAR_API_KEY:}
